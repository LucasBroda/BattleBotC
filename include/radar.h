#ifndef RADAR_H
#define RADAR_H

// Définition de la structure pour stocker les informations des objets
typedef struct {
    char* type;
    float position_x;
    float position_y;
} ObjectInfo;


#endif
